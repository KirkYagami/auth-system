# Vite exposes vars prefixed with VITE_ to the browser
# The proxy in vite.config.js handles /auth → backend in dev
# In production, set this to your deployed API base URL
VITE_API_BASE_URL=http://localhost:8000
